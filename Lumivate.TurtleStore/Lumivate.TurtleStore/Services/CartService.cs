using Lumivate.TurtleStore.Models;

namespace Lumivate.TurtleStore.Services
{
    // TODO (Module 5): Implement ICartService
    // 1. Add a constructor that accepts ITurtleService (dependency injection)
    // 2. Create a private static list of CartItem objects
    // 3. Implement GetCartItems(), AddToCart(), RemoveFromCart(), ClearCart(), GetCartTotal()
    // Hint: AddToCart should check if the turtle is already in the cart and increment quantity
    public class CartService : ICartService
    {
    }
}
